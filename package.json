{
  "name": "healthgent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix ui",
    "dev": "npm run dev --prefix ui",
    "start": "npm run start --prefix ui",
    "kb-pdf": "node scripts/export-kb-pdf.mjs"
  },
  "devDependencies": {
    "md-to-pdf": "^5.2.4"
  }
}
